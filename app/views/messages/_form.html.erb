<%= form_for(@message, :html => {:class => "messageForm panel panel-primary"}) do |f| %>
    <% if @message.errors.present? %>
        <div class="alert alert-danger">
            <%= alert %>
            <ul>
                <% @message.errors.full_messages.each do |full_message| %>
                <li>
                    <%= full_message %>
                </li>
                <% end %>
            </ul>
        </div>
    <% end %>
    <div class="form">
        <div class="form-group form-inline">
            <label for="messageName">名前 :</label>
            <%= f.text_field :name , :placeholder => "name" , class: "form-control" , id: "messageName" %>
            <label for="messageAge">年齢 :</label>
            <%= f.text_field :age , :placeholder => "age" , class: "form-control" , id: "messageAge" %>
        </div>
        <div class="form-group">
            <label for="messageText">内容 :</label>
            <%= f.text_area :body , :placeholder => "body" , :maxlength => "140" , class: "form-control" , id: "messageText" %>
        </div>
        <%= f.submit class: "btn btn-sm btn-primary" %>
    </div>
<% end %>