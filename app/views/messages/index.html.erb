<div class="panel panel-default messagePanel">
    <h1 class="h4">メッセージ一覧</h1>
    <% @messages.each do |message| %>
        <div class="panel-group">
            <div class="panel panel-info">
                <div class="panel-heading"><strong class="messagePanelName"><%= message.name %></strong><%= message.age %> 歳</div>
                <div class="panel-body">
                    <%= message.body %>
                    (<%= link_to "edit" , edit_message_path(message) %>
                     <%= link_to "destroy" , message , method: :delete, data: {confirm: '削除してもよろしいですか？' } %>
                    )
                </div>
            </div>
        </div>
    <% end %>
</div>

<%= render 'form' %>